# 代码审计核实结果（2026-09-25）

> 本文件是对 `docs/` 下三份 AI 审计报告的**独立核实**，方法为直接阅读源码 / 解包构建产物比对，未改动任何代码。
>
> **纳入**：`CODE-AUDIT-2026-09-25deepseek.md`、`code-audit-2026-09-25cursor.md`、`代码深度审计报告-2026-09-25GLM.md`
> **排除**：`audit-report-2026-09-25minimax.md`（旗舰 P0「登录无限流」经复核为**误报**，且其附带的"反向证据 grep"与代码不符；文件计数、迁移编号等亦有虚构，整体不予采信）
>
> 标注含义：
> - ✅ **已核实为真**：本次亲手读到对应代码/产物，证据行号附后
> - ⚠️ **部分真 / 需修正**：主体成立但措辞或评级需调整
> - ⬜ **未逐行复核**：报告提出、方向可信，但本次未打开对应文件确认（后续需要可补）
> - ❌ **证伪**：与代码实际不符

---

## 一、总体结论

三份报告对**"当前不可对公网发布"**的判断一致且成立。两处必须先修的阻断项——**Web 宿主任意文件读**与**控制平面零鉴权 + 回调 fail-open**——均已亲手复核为真。

可靠度排序（按本次抽样）：**GLM ≈ cursor > deepseek**。
- **GLM**：最全面，独家发现"CP 旧 jar 内仍嵌 e签宝 secret"，文件/迁移计数准确。
- **cursor**：评级校准最好，是唯一正确判断"登录限流确实存在"的报告，"已站住的控制"章节可信。
- **deepseek**：结构分析扎实，但 H8「登录无限流」为误报（见第三节）。

---

## 二、已核实为真（✅，附证据）

### 阻断级

| # | 论断 | 出处 | 证据 |
|---|------|------|------|
| 1 | Web 宿主 read 分支对任意真实路径放行（`readOnly:true`），可跨租户读 `auth.json`/`.env`/`/etc/passwd` | deepseek C1 / cursor C1 / GLM WEB-1 | `studio-web/server/host-core/services/files-api.ts:408-411`（write 才校验根，read 直接返回 real） |
| 2 | 控制平面全部签署端点零鉴权 | deepseek C4 / GLM CP-1 | `control-plane/pom.xml` 无 `spring-boot-starter-security`；`CpSigningController.java` 无鉴权注解 |
| 3 | e签宝回调验签 fail-open（缺头即跳过） | deepseek C6 / GLM CP-2 | `EsignCallbackController.java:60` `if (timestamp != null && signature != null)` |
| 4 | `SIGN_FLOW_UPDATE` 一律映射为"已签署" | GLM CP-3 | `EsignCallbackController.java:96` `yield "SIGNER_SIGNED"; // 简化：默认当作签署完成` |
| 5 | JWT 密钥默认空串；生产校验只在 profile **精确含 `prod`** 时触发，其余 `return` | deepseek C7 / cursor H2 | `application.yml:69 ${JWT_SECRET:}`；`ProductionConfigValidator.java` `if(!isProd) return;` |
| 6 | docker-compose 未设 `SPRING_PROFILES_ACTIVE` → 后端启动即崩 | GLM BE-1 / deepseek H10 / cursor M10 | `docker-compose.yml` backend 段无该变量；validator 无 profile 即 `throw` |
| 7 | 无配额行时自动写成 `PRO / 11000`，购买门禁 fail-open | cursor C2 / GLM BE-3 | `SignQuotaService.java:42-43` `setPlan("PRO"); setSignQuota(11000)` |
| 8 | 企业普通成员可把任意 userId 划进本租户（跨租户绑架） | cursor C3 / GLM BE-9 | `OrganizationController.java assignUserToCompany`：`hasCompanyAccess`（owner **或成员**均通过）+ `findById(任意userId)` + `bindUserToCompany` |
| 9 | CP 可部署旧 jar 内仍嵌 e签宝 secret 兜底值（**需立即在控制台轮换**） | GLM CP-4（独家） | `control-plane/target/control-plane-1.0.0.jar → BOOT-INF/classes/application.yml` `app-secret:...:15864da4...`；源码 yml 已改空 |

### 高危级

| # | 论断 | 出处 | 证据 |
|---|------|------|------|
| 10 | Windows 更新不验签 + 裸 IP 更新源 | deepseek C5 / cursor H7 / GLM FE-1 | `electron-builder.yml:114 verifyUpdateCodeSignature:false`、`:59 url: https://47.116.163.57:8082/latest`；`updater.ts:19` 同一 IP |
| 11 | 无 `safeStorage`/`keytar`，密钥明文落盘 | deepseek H4 / GLM FE-7 | `grep -rniE "safeStorage\|keytar"` 于 FE/WEB 源码 → 0 命中 |
| 12 | CORS 默认白名单含公网 IP `http://101.96.206.254:*` | cursor H3 / GLM | `application.yml:97` |
| 13 | 默认支付通道为 `simulated`（模拟确认即标已付） | cursor H1 | `application.yml:92 ${PAYMENT_PROVIDER:simulated}` |
| 14 | host-api `shell.openExternal/openPath` 无协议白名单（旧 IPC 通道却有） | GLM FE-2 | `shell-api.ts requireUrl` 仅校验非空；对比 `ipc-handlers.ts:1088` 有 `https:/http:` 白名单 |
| 15 | 知识库 `searchByKeywordPaged` 缺 `tenant_id` 过滤（跨租户检索） | deepseek H1 | `KnowledgeEntryRepository.java:47` 仅 `is_active=1`；对比 `:116 searchScopedForAgent` 有租户条件 |
| 16 | `approval_flow_id` 只写不读（审批流程可绕过） | deepseek H11 | 仅 `SignDraftService.java:109` 写入，无业务读取/强制点 |

### 仓库卫生 / 文档失真

| # | 论断 | 出处 | 证据 |
|---|------|------|------|
| 17 | `e签宝sdk/` 仍被 git 跟踪（虽已列入 `.gitignore`） | GLM 卫生 / deepseek | `git ls-files "e签宝sdk"` → **321** 个已跟踪文件（含两个大 zip） |
| 18 | predrop dump 与迁移备份目录滞留工作区、未被 `.gitignore` 覆盖 | cursor M9 / GLM | `backend/jy_financial_predrop_20260923_234601.sql`、`backend/db_migration_backup_20260923_234002/` 存在且未跟踪未忽略 |
| 19 | 全仓无 `.dockerignore` | deepseek C2/H9 | 根目录与 `studio-web/` 均不存在 |
| 20 | `TEST-REPORT.md` 数据失真 | deepseek 低 / GLM 文档漂移 | 实际 **323** 个 Java 文件、**13** 个迁移脚本（止于 V130）；报告称"333 文件 / 30 迁移(V001–V147)"→ 假 |
| 21 | admin 引导弱口令 `123456` 仍在配置（默认 `ENABLED:false`） | cursor L1 / GLM | `application.yml:104-108`，用户名实际为 `sssxuyi` |

---

## 三、证伪 / 需修正

| 论断 | 出处 | 结论 |
|------|------|------|
| 「登录无限流」 | deepseek H8（前半） | ❌ **证伪**。`AuthController.java:139` 调 `exceedsLoginRateLimit`→429；`SecurityCheckService` 实现 Redis IP 限流 + 账户锁定（`MAX_LOGIN_ATTEMPTS` 后 `lockAccount`）；`:180 setFailedLoginAttempts(0)`。cursor 的判断（IP 10 次、5 次锁定）才是对的。 |
| 登录响应存在**用户枚举面** | deepseek H8（后半） | ⚠️ **部分真**。用户不存在→"用户名或密码错误"；密码错误→"…剩余尝试次数:N"。两者文案不同，可据此探测账号是否存在。属低危，非"无限流"。 |

> 说明：minimax 把同一条"登录无限流"当作旗舰 **P0-2**，并附"grep `failedLoginAttempts` 仅命中 entity"的所谓反向证据——该 grep 与代码不符（AuthController 明确使用），是本次排除该报告的直接原因。

---

## 四、补验结果（原"未逐行复核"条目，现已全部打开源码确认）

| # | 论断 | 出处 | 结论 | 证据 |
|---|------|------|------|------|
| 22 | CP 签 HS256，DP 只验 RS256/JWKS，且 CP 无 `/cp/.well-known/jwks.json` 端点 → CP passport 在 DP 永远验不过（静默回退） | GLM CP-6 | ✅ **真** | CP `CpAuthController.java:56 sign(Algorithm.HMAC256(...))`；DP `CpTokenVerifier.java:70 require(Algorithm.RSA256(...))`、`:100` 拉 `/cp/.well-known/jwks.json`；CP 源码 `grep well-known\|jwks` → **0 命中** |
| 23 | 回调转发 DP 失败仅记日志，对 e签宝仍回 `code:0` → 停止重试、事件永久丢失 | GLM CP-8 | ✅ **真** | `EsignCallbackController.java forwardToDp` catch 仅 `log.error`；`callback()` 无视转发结果 `return Map.of("code",0,"msg","success")`；且 `dpCallbackToken` 为空时直接跳过转发仍回 code:0 |
| 24 | `eventKey` 取回调 body 的 `timestamp`，缺失退化为当前毫秒 → 幂等键不稳 | GLM CP-9 | ✅ **真** | `EsignCallbackController.java:75` `body.path("timestamp").asText(String.valueOf(System.currentTimeMillis()))` |
| 25 | platform-proxy 对后端 401 不刷新 token，直接透传 | GLM WEB-2 | ✅ **真** | `studio-web/server/src/platform-proxy.ts:59` `reply.code(res.status).send(data)`，无 refresh 分支 |
| 26 | Overview 上传用 `String.fromCharCode(...spread)`，大文件必栈溢出；文案称 ≤50MB 但无校验 | GLM WEB-3 | ✅ **真** | `studio-web/src/pages/Overview/index.tsx:42` `btoa(String.fromCharCode(...new Uint8Array(buf)))`；`:108` hint "≤50MB"，无 size 校验 |
| 27 | 顶栏租户/用户身份写死 | GLM WEB-4 | ✅ **真** | `TopBar.tsx:18 const TENANTS=['君言律师事务所','个人空间']`；`:110 title="许一 · ADMIN"` |
| 28 | 改密不吊销既有会话 / refresh token | GLM BE-11 | ✅ **真** | `UserController.java:215 changePassword` 仅 `updatePassword` + 返回"请重新登录"，无任何 token/session 吊销调用 |
| 29 | 邮箱验证码是空壳（从不发送却记"sent"） | GLM BE-12 | ✅ **真** | `EmailVerificationService.java:175 sendEmailMessage` 内 `// TODO: 实际调用邮件API` + 注释掉的 `mailService.sendEmail`，仅 `log.info("...sent...")` |
| 30 | `/internal/tools/**` 不在权益门禁内 | GLM M | ✅ **真** | `WebConfig.java:45-49` EntitlementInterceptor 仅覆盖 `/api/sign/** /api/evidence/** /api/templates/** /api/review/**` |
| 31 | Excel 导入 `@Async` 自调用致 `@Transactional` 代理失效 + catch 不打日志 | deepseek 8 / GLM BE-10 | ✅ **真** | `ExcelImportService.java:85 @Async startImportAsync` → `:87 runImport(jobId)`（this. 自调用）；`:90 @Transactional runImport` 代理被绕过；`:124 catch` 只 `setErrorSummary`，无 `log.error` |
| 32 | `contract_template`/`evidence_item` 缺 `IF NOT EXISTS` 与 `COLLATE`（落默认 `utf8mb4_0900_ai_ci`，与全库 `utf8mb4_unicode_ci` 不一致） | deepseek H13 | ✅ **真** | `V130__legal.sql:390 CREATE TABLE contract_template (`（无 IF NOT EXISTS）、`:402 ...CHARSET=utf8mb4;`（无 COLLATE）；`:407 CREATE TABLE evidence_item (` 同；对比 `:15/:80` 等表均有 `IF NOT EXISTS` + `COLLATE=utf8mb4_unicode_ci` |
| 33 | 桌面应用页无 CSP；主窗口导航放行任意 `https:` | deepseek C3 | ✅ **真** | `electron/main/index.ts:372-373 onHeadersReceived` 只对 `urls:['http://127.0.0.1:18789/*','http://localhost:18789/*']` 生效（不含应用自身 `file://`）；`:662 will-navigate` → `:672 isHttps = protocol==='https:'`、`:674 if(!isFile&&!isHttp&&!isHttps)` 才拦 → 任意 https 放行，无域名白名单 |
| 34 | `chargeForSigning` 并发不安全 → 双扣费 / 重复建流程 | GLM BE-4 | ⚠️ **部分真（措辞需修正）** | 存在幂等护栏 `SignTaskController.java:165 if(providerFlowId!=null&&!isBlank) return;`，**并非"无幂等"**；但为读后写、无锁，且 `provider_flow_id` **无唯一索引**（`V130:478 VARCHAR(128) NULL`，仅 `uk_sign_provider_event` 在 event_key）→ 并发 SUBMIT 仍可双双越过护栏 |
| 35 | 先扣费后转移、失败无补偿 | GLM BE-5 | ⚠️ **部分真** | `transition:119` 先 `chargeForSigning` 再 `flow.transition`；失败无回滚/对账 → 属实；但因 #34 护栏，重试**不会**二次扣费（GLM 未点明此缓解） |
| 36 | `/transition SUBMIT` 绕过草稿校验，可对空任务扣费建流程 | GLM BE-6 | ⚠️ **未完全证实** | `transition:118` 扣费前已调 `flow.authorizeTransition(id,trigger,userId)`（注释"授权先行含状态机边预检"）；但该预检是否校验"参与方/文档非空"未读到实现，暂不能确认 GLM 的"空任务扣费"结论 |
| 37 | V130 合并 + 就地改写已应用迁移 → 既有库 checksum mismatch，不可原地升级 | GLM BE-2 | ⚠️ **部分真** | 合并属实（`V130:387 "合并自 V140"`、`:404 "合并自 V141"`，且 `validate-on-migrate:true`）；但"V001/V110 被就地改写"未经 git 历史比对确认，逻辑推论成立、直接证据待补 |

---

## 五、建议的修复顺序（综合三份报告，取交集与最高危）

**P0（本周，部署阻断）**
1. CP 鉴权层 + 回调改 fail-closed（掐断公网伪造签署链）— 论断 2/3/4
2. `mvn clean` 删 CP 旧 jar + e签宝控制台轮换 secret — 论断 9
3. Web 宿主 read 分支补 `isPathInside(用户根 || 授权 workspace)` — 论断 1
4. docker-compose/Dockerfile 加 `SPRING_PROFILES_ACTIVE=prod`；prod 下禁 `simulated` 支付 — 论断 6/13
5. 缺配额行改为 FREE / 额度 0，不在读路径升级套餐 — 论断 7
6. org assign 仅限所有者，拒绝把已属其它非空租户的用户划入 — 论断 8

**P1（两周内）**
7. CORS 默认值删 `101.96.206.254`，改环境变量注入、prod 缺失即 fail — 论断 12
8. 更新包代码签名 + 更新源域名化 — 论断 10
9. host-api shell 通道补协议白名单（复用旧 IPC 的实现）— 论断 14
10. 知识库检索补 `tenant_id` — 论断 15；密钥走 safeStorage — 论断 11
11. `approval_flow_id` 补齐读取与强制点 — 论断 16

**P2（结构性）**
12. 四端 0 自动化测试、0 CI：优先给配额/支付/签署状态机/租户隔离补集成测试
13. 清理仓库卫生（dump/备份移出、`.gitignore` 补 `*.sql` 与备份目录、`e签宝sdk` `git rm --cached`）— 论断 17/18
14. 修正 `TEST-REPORT.md` 失实数据 — 论断 20

---

## 六、本次已修复（P0，2026-09-25）

以下为已落地并通过编译/类型检查/lint 的改动（backend `mvn compile` ✅、control-plane `mvn clean compile` ✅、studio-web `typecheck:server` + `eslint` ✅）。

| 论断 | 修复内容 | 改动文件 |
|------|----------|----------|
| #1 C1 任意文件读 | read 分支不再放行任意路径，收敛到本账号 per-scope 根；`stagePaths` 复制前校验；media `thumbnails`/save 复用同一守卫；legacy IPC `file:*` 重复实现同步收敛 | `studio-web/server/host-core/services/files-api.ts`、`.../media-api.ts`、`.../main/ipc-handlers.ts` |
| #2/#3/#4 CP 零鉴权 + 回调 fail-open | 新增 `CpSigningAuthInterceptor` 强制校验 `/cp/v1/signing/**` 的 Bearer JWT（HS256+issuer+exp）；回调改 fail-closed（缺签名头即拒）+ ±5 分钟时间窗防重放 | `control-plane/.../security/CpSigningAuthInterceptor.java`、`.../config/WebConfig.java`、`.../controller/EsignCallbackController.java` |
| #9 CP jar 泄漏 secret | `mvn clean` 删除含 secret 兜底值的旧 fat jar（target 已 gitignore，属本地产物）；**仍需人工在 e签宝控制台轮换 app-secret**（历史曾入库） | — |
| #21 弱默认口令 | CP `application.yml` 移除 `cp-secret`/`cp-dev-jwt-secret...`/`dev-callback-token` 兜底；新增 `CpSecretGuard` 非 dev profile 下密钥缺失/过短/弱值即 fail-fast（否则已知默认可伪造 passport 绕过 #2 鉴权）；backend 移除 `ADMIN/USER_BOOTSTRAP_PASSWORD: "123456"` | `control-plane/.../config/CpSecretGuard.java`、`control-plane/src/main/resources/application.yml`、`backend/src/main/resources/application.yml` |
| #13 模拟支付 | `ProductionConfigValidator` 在 prod 拒绝 `payment.provider=simulated`/空（simulate-confirm 端点已自带 `isSimulated()` 门禁，此前唯一缺口就是 prod 默认 simulated） | `backend/.../core/config/ProductionConfigValidator.java` |
| #6 compose profile | backend 加 `SPRING_PROFILES_ACTIVE: ${...:-dev}`（可覆盖为 prod）+ `CP_MODE: ${...:-local}`；studio-web 移除 `JWT_SECRET` 弱兜底改为强制注入；补根 `.dockerignore`（阻止 `.env`/`node_modules`/dump/sdk 进镜像，修复 C2） | `docker-compose.yml`、`.dockerignore`、`studio-web/Dockerfile` |
| #7 配额默认 PRO | `SignQuotaService.quotaOf` 缺行时改插 `FREE/0`（不再在读路径升级套餐） | `backend/.../account/service/SignQuotaService.java` |
| #8 org 跨租户绑架 | `assignUserToCompany` 改用 `isCompanyOwnerOrAdmin`（仅 owner/管理员，普通成员不放行）+ 拒绝把已属其它非空租户的用户划入 | `backend/.../auth/controller/OrganizationController.java` |

**未在本次处理（需人工/后续）**：
- **#9 密钥轮换**：`mvn clean` 只删本地产物；`ESIGN_APP_SECRET`（`15864da4...`）曾在源码入库，**必须在 e签宝控制台轮换**——这是控制台操作，代码侧无法完成。
- **桌面 CSP（deepseek C3 的 CSP 部分）**：`index.html` 含内联 `<script>`（主题引导、crypto polyfill）与内联 `<style>`（首屏加载动画），严格 CSP（`script-src 'self'`）会直接阻断这些内联脚本/样式而白屏。正确做法是把内联脚本外置 + nonce/hash，且必须在真实运行的 Electron 中回归——无法在此环境盲改。已改为先落地 C3 的**导航白名单**部分（见下），配合已有"无 dangerouslySetInnerHTML/eval"，XSS→提权链已大幅收敛。
- **更新包代码签名（C5/H7/FE-1）**：需购买代码签名证书并把更新源域名化，属采购/运维动作，代码侧无法完成。
- **safeStorage 密钥加密（H4/P1-3/FE-7）**：需引入 Electron `safeStorage` 并迁移既有明文存储，改动面大且需运行时验证，留待专项。
- **会话吊销（BE-11/H6）**：改密后吊销既有 refresh token 需引入 token 版本号/黑名单机制，属结构性改动。

## 七、第二轮已修复（P0 尾项 + P1，2026-09-25）

均通过编译/类型检查/lint：backend `mvn compile` ✅、control-plane `mvn compile` ✅、studio-frontend `typecheck`+`eslint` ✅、studio-web `typecheck:server`+`eslint` ✅。

| 论断 | 修复内容 | 改动文件 |
|------|----------|----------|
| CP-3 | `SIGN_FLOW_UPDATE` 不再一律判"已签署"：依据 e签宝 SDK `CallbackCheckDemo` 的 body schema，仅 `signResult==2`（签署完成）映射 `SIGNER_SIGNED`，其余子事件 IGNORE 不转发 | `EsignCallbackController.java` |
| BE-4 | `chargeForSigning` 加 taskId 分段锁（单实例串行化，锁内以库中最新状态复检 providerFlowId）+ 新增 `V150` 迁移给 `provider_flow_id` 加唯一索引（跨实例兜底） | `SignTaskController.java`、`db/migration/V150__sign_task_provider_flow_unique.sql` |
| H3/P0-1 | CORS 默认值移除公网 IP `http://101.96.206.254:*`，仅留本地开发源；生产由 `APP_CORS_ALLOWED_ORIGINS` 注入 | `backend/.../application.yml` |
| H1 | 知识库 `searchByKeywordPaged` 补 `(tenant_id IS NULL OR tenant_id = :tenantId)` 租户过滤（query+countQuery），`KnowledgeService` 传入 `JyTenantContext.get()` | `KnowledgeEntryRepository.java`、`KnowledgeService.java` |
| FE-2/P1-1 | host-api `shell.openExternal` 的 `requireUrl` 加 http/https 协议白名单（与旧 IPC 通道对齐），FE 与 WEB 两份同改 | `studio-frontend/electron/services/shell-api.ts`、`studio-web/server/host-core/services/shell-api.ts` |
| C3（导航部分） | 桌面 `will-navigate` 收紧：仅允许 app 目录内 `file://` 与 `localhost/127.0.0.1` http；外链改由 `setWindowOpenHandler`→`openExternal` 走系统浏览器，受信主窗口不再被切到任意外部 https 源 | `studio-frontend/electron/main/index.ts` |
| deepseek H8（枚举部分） | 登录失败对"用户不存在"与"密码错误"返回一致文案，移除"剩余尝试次数"回传，消除账号枚举面（失败计数仍记服务端） | `AuthController.java` |

## 八、第三轮已修复（P1，2026-09-25）

backend `mvn compile` ✅、studio-web `typecheck:server` + `eslint` ✅。

| 论断 | 修复内容 | 改动文件 |
|------|----------|----------|
| H5 | Web 端 legacy `provider:getApiKey` 改为掩码（`****last4`）与桌面端一致；内部真实取值仍走 host-api `providers.getApiKey`（未动），不影响 Gateway 配置 | `studio-web/server/host-core/main/ipc-handlers.ts` |
| H12 / cursor H5 | `buildOrderItem`：`skuId` 显式提供时，SKU 不存在→拒单（不再静默回退商品价且不扣库存）；`sku.productId != productId`→拒单（堵廉价 SKU 配高价商品名绕过定价） | `OrderService.java` |
| H7（Origin 部分） | `/ws` 增加 Origin 校验（CSWSH）：同源 / `WS_ALLOWED_ORIGINS` 白名单 / 非生产下 localhost 放行，其余 `close(4403)`；新增 `WS_ALLOWED_ORIGINS` 配置 | `studio-web/server/src/index.ts`、`env.ts` |

**评估后未改（需产品/结构设计，不盲改）**：
- **H11 审批流强制点**：`approval_flow_id` 全仓仅 `SignDraftService` 写入、无任何读取/校验点；且后端**没有独立的审批流实体**（只有 DingTalk/Feishu/WeCom 的 `*ApprovalMapping` 集成映射）。该字段是指向外部审批系统的裸引用，缺少"已审批"状态契约。**贸然加"SUBMIT 前必须审批通过"的硬门禁会阻断全部签署**（无处产生可校验的已审批态）。需先确定审批模型（自建 or 绑定三方、状态字段、豁免条件）再实现。
- **H7 令牌进 URL（M7）**：`?token=` 供 iframe 模式使用，改为一次性短时票据需新增签发/消费端点并改 iframe 客户端，属结构性改动，本轮只做 Origin 校验；令牌进 URL 的日志/历史泄露面留待票据化。
- **无 SKU 商品不扣库存**（deepseek H12 的另一半）：`Product` 无库存字段，商品级库存扣减需要 schema + 仓储改动，未在本轮实现。
